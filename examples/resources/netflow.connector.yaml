# Name of a connector. Must be unique among connectors.
name: netflow

# Connector kind.
kind: netflow

# Listen address and port.
url: :1515

# Buffer size. Default is 8192 bytes.
buffer_size: 12288
